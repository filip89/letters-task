import { GameMap } from './models/GameMap.ts';
import { defaultMaps } from './constants/defaultMaps.ts';

//Set the map to see the results in your browser
//You can use any of the default maps or set your own
export const activeMap: GameMap = defaultMaps.basic.map;
